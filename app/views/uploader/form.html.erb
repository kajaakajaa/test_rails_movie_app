<%
=begin%>
 <% @upload = UploadFile.new %>
  <%= form_for @upload, :url => {:action => :upload} do |f| %>
    <%= f.label :name, "name" %>
    <%= f.text_field :name %>
    <%= f.label :file %>
    <%= f.file_field :file %>
    <%= f.submit "Upload" %>
<% end %> 
<%
=end%>

 <% @upload = UploadFile.new %>
  <% form_with model: @upload, url: uploader_form_path, id: "new_upload", class: "new_upload", local: true do |f| %>
    <%= f.label :name, "name" %>
    <%= f.text_field :name %>
    <%= f.label :file %>
    <%= f.file_field :file %>
    <%= f.submit "Upload" %>
  <% end %> 

<p>aaa</p>